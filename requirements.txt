# Industrial Defect Detection Desktop Software
# NOTE: torch / torchvision are NOT listed here.
# Please run this inside an environment where `import torch` already works.
PySide6-Essentials>=6.8
requests>=2.32
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.12
pydantic>=2.9

# ---------------------------------------------------------------------------
# The detection model OEGT-CP is installed separately from its GitHub repo:
#   pip install -e "path/to/OEGT-CP"
#   or: pip install "git+https://github.com/lyylovemwj/OEGT-CP.git"
# It will pull its own deps (opencv-python, transformers, etc.).
# ---------------------------------------------------------------------------
